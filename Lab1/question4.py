# Write a Python program which accepts the radius of a circle from the user and compute the area. (Hint: import math and use math.pi)

import math

radius = float(input("Enter Radius :"))

area = 3.14 * radius * radius

print(f"Area of circle = {area}")